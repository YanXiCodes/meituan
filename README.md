# 🤖 美团听单系统 (Meituan Order Listening System)

> 一个基于微信的自动化订单处理系统，通过 PC 微信 + AI 驱动的机器人大脑，实现订单自动抓取与智能回复。

---

## 📋 项目概述

### 核心功能
- ✅ **实时订单监听** - WebSocket 监听美团/饿了么订单
- ✅ **自动消息转发** - 订单信息实时推送到微信群
- ✅ **管理员控制** - 支持启动/停止监听命令
- ✅ **一键启动** - 自动化启动所有后台服务

### 系统架构

```
┌──────────────────────────────────────┐
│  美团/饿了么平台                      │
│  (WebSocket 订单推送)                 │
└────────────┬─────────────────────────┘
             │ WebSocket
┌────────────▼──────────────────────────┐
│  机器人大脑 (NoneBot2)                 │
│  - 订单监听与解析                      │
│  - 指令处理（开/关）                   │
│  - 消息转发控制                        │
└────────────┬──────────────────────────┘
             │ OneBot12 Protocol
┌────────────▼──────────────────────────┐
│  微信连接器 (ComWeChatBotClient)      │
│  - 消息下发到微信                      │
│  - 群组管理                            │
└────────────┬──────────────────────────┘
             │ COM 接口
┌────────────▼──────────────────────────┐
│  PC 微信客户端 (手动登录)              │
│  ComWeChatRobot                       │
└───────────────────────────────────────┘
```

### 项目结构

```
meituan/
├── launcher.py                    # 🚀 一键启动脚本 (Windows/Conda)
├── ComWeChatBotClient/            # 微信连接器中间件
│   ├── main.py                    # 入口点
│   ├── pyproject.toml             # Poetry 依赖配置
│   ├── requirements.txt           # pip 依赖
│   └── wechatbot_client/          # 核心代码
│       ├── config.py              # 配置管理
│       ├── consts.py              # 常量定义
│       ├── log.py                 # 日志模块
│       ├── com_wechat/            # 微信 COM 接口
│       ├── action_manager/        # 消息动作处理
│       ├── driver/                # 网络驱动
│       ├── onebot12/              # OneBot12 适配器
│       └── wechat/                # 微信协议实现
├── wechatbot/wechat/              # 机器人大脑 (NoneBot2)
│   ├── pyproject.toml             # 项目配置
│   └── src/
│       ├── core/                  # 核心业务逻辑
│       └── reply/                 # AI 回复模块
├── log/                           # 日志输出目录
│   ├── debug/
│   ├── info/
│   └── error/
└── file_cache/                    # 文件缓存目录
    ├── image/
    ├── voice/
    └── temp/
```

---

## 🚀 快速开始

### 前置需求

| 项目 | 版本/要求 | 备注 |
|------|---------|------|
| **Windows** | 10/11 | 仅支持 Windows |
| **Python** | 3.10+ | 建议 3.10 或 3.11 |
| **Conda** | 最新版 | 用于环境隔离 |
| **PC 微信** | 3.7.0.30 | [下载链接](https://github.com/tom-snow/wechat-windows-versions/releases/download/v3.7.0.30/WeChatSetup-3.7.0.30.exe) |
| **美团账号** | 商家账号 | 用于授权 WebSocket 连接 |

### 安装步骤

#### 1️⃣ 克隆项目
```powershell
git clone https://github.com/YanXiCodes/meituan.git
cd meituan
```

#### 2️⃣ 创建 Conda 环境
```powershell
# 创建环境
conda create -n meituan python=3.10

# 激活环境
conda activate meituan
```

#### 3️⃣ 安装微信连接器依赖
```powershell
cd ComWeChatBotClient

# 安装 Python 依赖
pip install -r requirements.txt
# 或使用 Poetry
poetry install

cd ..
```

#### 4️⃣ 安装机器人大脑依赖
```powershell
cd wechatbot/wechat

# 安装 NoneBot2 框架和插件
poetry install

cd ../..
```

#### 5️⃣ 配置监听参数

编辑 `wechatbot/wechat/src/reply/__init__.py`：

```python
# 第 13-14 行：设置监听的微信群 ID
GROUP_ID = "43754149608@chatroom"  # 更改为你的目标群组 ID

# 第 17-18 行：设置管理员账号（只有这些账号可以发送开/关命令）
ADMIN_USER_IDS = {"wxid_u9377ranh6rz22", "wxid_h8l54q5wx7s122"}

# 第 21-23 行：调整消息延迟（模拟人类操作速度）
HUMAN_DELAY_MIN = 0.3   # 最小延迟（秒）
HUMAN_DELAY_MAX = 0.3   # 最大延迟（秒）
COOLDOWN_SECONDS = 5.0  # 冷却时间（秒）
```

编辑 `wechatbot/wechat/src/core/main.py`：

```python
# 第 13-14 行：设置美团 WebSocket 连接信息
WEBSOCKET_URL = "wss://pikem0-bj.sankuai.com/pike/?bizId=msg_pike_client_dz&EIO=3&transport=websocket"

# 在 HEADERS 中更新你的 Cookie（从美团平台获取）
HEADERS = {
    "Cookie": "your_cookie_here",
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64)..."
}
```

---

## ▶️ 运行项目

### 方式 A：自动启动（推荐）

**Windows PowerShell：**
```powershell
conda activate meituan
python launcher.py
```

**cmd 命令行：**
```cmd
conda activate meituan
python launcher.py
```

### 方式 B：手动启动

#### 启动微信连接器
```powershell
conda activate meituan
cd ComWeChatBotClient
python main.py
```

#### 启动机器人大脑（新终端）
```powershell
conda activate meituan
cd wechatbot/wechat
nb run
```

---

## 🔧 配置说明

### launcher.py 配置

如果你的项目路径不同，需要修改 `launcher.py`：

```python
# 第 7-9 行
CONDA_ENV_NAME = "meituan"                              # Conda 环境名
WECHAT_BOT_CLIENT_PATH = "D:\\meituan\\ComWeChatBotClient"  # 连接器路径
NONEBOT_PROJECT_PATH = "D:\\meituan\\wechatbot\\wechat"    # 机器人大脑路径
```

### ComWeChatBotClient 配置

编辑 `ComWeChatBotClient/wechatbot_client/config.py`

主要配置项：
```python
# HTTP 服务配置
host = "127.0.0.1"
port = 8000

# 消息处理
message_cache_size = 1000
max_retry = 3

# 文件管理
file_cache_dir = "./file_cache"
max_cache_size = 1000  # MB
```

### NoneBot2 配置

编辑 `wechatbot/wechat/pyproject.toml`

```toml
[tool.nonebot]
# OneBot 适配器
adapters = [
    { name = "OneBot V12", module_name = "nonebot.adapters.onebot.v12" }
]

# 插件目录
plugin_dirs = ["src"]
```

---

## 📊 日志与调试

### 日志位置
```
log/
├── debug/      # 调试日志（最详细）
├── info/       # 信息日志
└── error/      # 错误日志
```

### 查看实时日志

**微信连接器日志：**
```powershell
Get-Content .\ComWeChatBotClient\log\*.log -Tail 50 -Wait
```

**机器人大脑日志：**
```powershell
Get-Content .\wechatbot\wechat\*.log -Tail 50 -Wait
```

### 调试模式

修改 `launcher.py` 或各模块的启动命令：
```powershell
# 连接器调试
python main.py --debug

# 机器人调试
nb run --reload
```

---

## 🆘 常见问题

### Q1: 启动时提示找不到 Conda 环境
**解决方案：**
```powershell
# 检查环境是否存在
conda env list

# 如果没有，重新创建
conda create -n meituan python=3.10 -y
```

### Q2: WebSocket 连接失败或无订单推送

**解决方案：**
1. 检查 `src/core/main.py` 中的 Cookie 是否有效
2. 确认美团账号权限正常
3. 检查网络连接和防火墙设置
4. 在浏览器中访问美团后台，确认能正常操作

### Q3: NoneBot 找不到适配器
**解决方案：**
```powershell
# 重新安装依赖
cd wechatbot/wechat
poetry install
poetry add nonebot2[fastapi]
```

### Q4: 命令无响应（开/关不工作）

**解决方案：**
1. 确认发送命令的微信账号在 `ADMIN_USER_IDS` 中
2. 检查群组 ID 是否正确（`GROUP_ID`）
3. 查看日志中是否有错误信息

---

### 📱 使用场景

### 典型工作流
```
1. 美团/饿了么 有新订单
   ↓
2. WebSocket 接收到订单推送
   ↓
3. 机器人解析订单信息
   ↓
4. 自动发送到微信群
   ↓
5. 管理员在群里接收订单（支持开/关监听）
```

### 支持的命令
- `开` - 启动订单监听（仅管理员可用）
- `关` - 停止订单监听（仅管理员可用）

---

## 🛠️ 维护与升级

### 定期检查

每月进行以下检查：
```powershell
# 检查日志是否有大量错误
Get-ChildItem log/ -Recurse -Filter "*.log" | 
  ForEach-Object { 
    $errors = @(Select-String -Path $_.FullName -Pattern "ERROR")
    if ($errors.Count -gt 0) { Write-Host "⚠️  $($_.Name): $($errors.Count) errors" }
  }

# 检查缓存大小
(Get-ChildItem file_cache/ -Recurse -File | 
  Measure-Object -Sum Length).Sum / 1GB
```

### 更新依赖

```powershell
# 更新 ComWeChatBotClient 依赖
cd ComWeChatBotClient
pip install --upgrade -r requirements.txt

# 更新 NoneBot 依赖
cd ../wechatbot/wechat
poetry update

cd ../..
```

### 清理缓存

```powershell
# 清理旧的文件缓存
Remove-Item -Path file_cache/* -Recurse -Force

# 清理旧的日志（保留最近 30 天）
Get-ChildItem log/ -Recurse -Filter "*.log" | 
  Where-Object { $_.LastWriteTime -lt (Get-Date).AddDays(-30) } | 
  Remove-Item -Force
```

---

## 🔐 安全建议

- ⚠️ **不要提交 `.env` 文件**（已在 `.gitignore` 中）
- ⚠️ **定期更换 OpenAI API 密钥**
- ⚠️ **仅在信任的网络中运行**
- ⚠️ **启用 Windows 防火墙**
- ⚠️ **定期备份日志和数据**

---

## 📝 许可证

本项目采用 **AGPLv3** 协议开源，仅供个人学习使用。

---

## 📞 技术栈参考

| 组件 | 项目 | 文档 |
|------|------|------|
| **微信接口** | [ComWeChatRobot](https://github.com/ljc545w/ComWeChatRobot) | [README](ComWeChatBotClient/README.md) |
| **OneBot 协议** | [OneBot12](https://onebot.dev/) | [文档](ComWeChatBotClient/docs/) |
| **机器人框架** | [NoneBot2](https://nonebot.dev/) | [官方文档](https://nonebot.dev/) |
| **AI 引擎** | [OpenAI API](https://openai.com/api/) | [API 文档](https://platform.openai.com/docs/) |

---

## 🎯 未来改进计划

- [ ] 支持 Linux/MacOS
- [ ] Web 管理后台
- [ ] 数据库持久化（目前基于文件）
- [ ] Docker 容器化部署
- [ ] 性能监控与告警
- [ ] 多账号支持

---

## 📅 版本历史

### v1.0.0 (2025-11-15)
- ✅ 初始版本发布
- ✅ 完成微信连接器集成
- ✅ 实现 NoneBot2 机器人大脑
- ✅ 编写完整部署文档

---

## 💡 开发者笔记

此文档的目的是让你在 **多年后** 依然能够快速重新启动这个系统。

如果你在运行过程中发现新问题，**请更新本文档**，以便：
1. 帮助未来的你快速解决
2. 帮助其他想部署此系统的人

**关键更新点：**
- 🔄 系统依赖版本变化
- 🐛 新发现的 Bug 和解决方案
- ⚡ 性能优化技巧
- 🔧 新增功能说明

---

> ⏰ 最后更新：2025年11月15日  
> 👤 维护者：YanXiCodes  
> 🏠 仓库：https://github.com/YanXiCodes/meituan
