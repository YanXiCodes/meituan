| 特征 | T5：暴力遍历 (Brute-force Traversal) | T6：动态规划优化 (DP Optimization) |\\n| :--- | :--- | :--- |\\n| \*\*实现方法\*\* | 每次查询时，都从头开始\*\*深度递归遍历\*\*整个目录子树，实时累加所有文件的大小。 | \*\*首次查询\*\*时进行递归计算，但将结果\*\*存入缓存\*\* (\`cached\_size\`)。<br>\*\*后续查询\*\*直接返回缓存值。 |\\n| \*\*优点\*\* | <ul><li>逻辑简单，易于理解和实现。</li><li>数据永远是实时的，不会有偏差。</li><li>内存占用低，无需额外存储。</li></ul> | <ul><li>\*\*重复查询效率极高\*\*，接近O(1)时间复杂度。</li><li>性能提升巨大，用户体验好。</li><li>通过动态更新，大部分情况下能维护数据准确性。</li></ul> |\\n| \*\*缺点\*\* | <ul><li>\*\*性能极差\*\*，查询大目录时非常缓慢。</li><li>对于重复查询，会进行大量不必要的重复计算。</li><li>CPU消耗大。</li></ul> | <ul><li>增加了代码的\*\*实现复杂度\*\*。</li><li>以\*\*空间换时间\*\*，增加了内存消耗。</li><li>更新逻辑若有疏漏，可能导致缓存数据与实际不符（数据陈旧）。</li></ul> |\\n| \*\*核心思想\*\* | \*\*即用即算\*\* | \*\*一次计算，多次复用\*\* |
